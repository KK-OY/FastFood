<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShopCartMapper">
    <insert id="addShopCart">
        insert into shopping_cart
            ( name, image, user_id, dish_id, setmeal_id, dish_flavor, number, amount, create_time)
        VALUES (#{name},#{image},#{userId},#{dishId},#{setmealId},#{dishFlavor},#{number},#{amount},#{createTime})
    </insert>
    <update id="updateShopCart">
        update shopping_cart <set>
        number = #{number},
    </set>
        where id = #{id}
    </update>
    <delete id="delShopCart">
        delete from shopping_cart where id = #{id}
    </delete>
    <delete id="cleanShopCart">
        delete from shopping_cart where user_id = #{userId}
    </delete>

    <select id="findShopCart" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart <where>
         user_id =#{userId}
        <if test="dishId !=null"> and dish_id = #{dishId}</if>
        <if test="setmealId !=null">and setmeal_id = #{setmealId}</if>
        <if test="dishFlavor !=null">and dish_flavor = #{dishFlavor}</if>
    </where>
        LIMIT 1
    </select>
    <select id="getShopCart" resultType="com.sky.entity.ShoppingCart">
        SELECT * FROM shopping_cart WHERE user_id = #{userId}
    </select>
</mapper>
